[project]
name = "vuln-fixture"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests==2.25.1",
    "flask==2.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
