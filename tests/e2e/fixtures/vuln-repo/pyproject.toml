[project]
name = "vuln-fixture"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests==2.33.0",
    "flask==3.1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
