"""Clean application — no vulnerabilities, no code smells."""


def greet(name: str) -> str:
    """Return a greeting message."""
    return f"Hello, {name}!"


def add(x: int, y: int) -> int:
    """Add two integers."""
    return x + y
